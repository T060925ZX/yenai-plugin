# 邮箱验证码入群验证使用说明

## 功能介绍

新增了邮箱验证码入群验证功能，用户进群时会自动向其QQ邮箱发送验证码，用户在群内输入验证码即可完成验证。

## 安装依赖

首先需要安装 nodemailer 邮件发送库：

```bash
npm install nodemailer
```

## 配置步骤

### 1. 配置SMTP服务

在锅巴配置界面或配置文件中设置SMTP信息：

#### 使用锅巴配置（推荐）
1. 进入锅巴配置界面
2. 找到"群管配置" -> "SMTP邮件服务配置"
3. 填写以下信息：
   - **SMTP服务器地址**：根据你的邮箱服务商填写
     - QQ邮箱: `smtp.qq.com`
     - 163邮箱: `smtp.163.com`
     - Gmail: `smtp.gmail.com`
   - **SMTP端口**: 通常为 465 (SSL) 或 587 (TLS)
   - **使用SSL连接**: 开启
   - **发件人邮箱地址**: 你的邮箱地址
   - **邮箱授权码**: 在邮箱设置中获取（不是登录密码！）
   - **发件人显示名称**: 例如"椰奶机器人"

#### 手动编辑配置文件
编辑 `config/default_config/groupAdmin.yaml`，修改 `smtpConfig` 部分。

### 2. 获取邮箱授权码

#### QQ邮箱获取授权码步骤：
1. 登录QQ邮箱网页版
2. 点击"设置" -> "账户"
3. 找到"POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务"
4. 开启"IMAP/SMTP服务"
5. 点击"生成授权码"
6. 按提示发送短信，获取授权码
7. 将授权码填入配置中

#### 163邮箱获取授权码步骤：
1. 登录163邮箱网页版
2. 点击"设置" -> "POP3/SMTP/IMAP"
3. 开启"IMAP/SMTP服务"
4. 点击"客户端授权密码"
5. 设置并获取授权码

### 3. 切换验证类型

使用命令切换到邮箱验证模式：

```
#切换验证类型
```

会在"数学计算"和"邮箱验证码"两种模式之间切换。

## 使用流程

1. **用户进群**：系统自动向用户的QQ邮箱（QQ号@qq.com）发送验证码邮件
2. **用户查收邮件**：用户登录自己的QQ邮箱查看验证码
3. **输入验证码**：用户在群内输入收到的6位数字验证码
   - ⚠️ 注意：如果发送的不是6位数字，消息会被自动撤回并收到警告
   - ⚠️ 注意：请勿在验证期间发送其他内容
4. **验证成功**：系统确认验证码正确，允许用户留在群内

## 配置项说明

### SMTP配置 (smtpConfig)
- `host`: SMTP服务器地址
- `port`: SMTP端口（465或587）
- `secure`: 是否使用SSL连接
- `user`: 发件人邮箱地址
- `pass`: 邮箱授权码（重要：不是登录密码）
- `fromName`: 发件人显示名称

### 邮箱验证配置 (emailVerify)
- `codeLength`: 验证码长度（默认6位）
- `maxAttempts`: 最多允许尝试次数（默认5次）
- `emailSubject`: 邮件主题（支持变量替换）
- `emailTemplateStyle`: 邮件模板风格（见下方说明）

### 内置邮件模板风格

系统提供了5种精美的内置模板供选择，还支持自定义模板：

| 模板风格 | 值 | 特点 |
|---------|-----|------|
| 渐变紫色 | `gradient-purple` | 现代时尚，渐变紫头部，默认推荐 ✨ |
| 简约蓝色 | `simple-blue` | 清爽简洁，蓝色主题，适合商务 |
| 商务卡片 | `business-card` | 专业商务，卡片式设计，层次分明 |
| 暗色科技 | `dark-tech` | 酷炫科技感，深色主题，霓虹蓝 |
| 清新绿色 | `fresh-green` | 自然清新，绿色渐变，活力满满 🌿 |
| **自定义** | `custom` | **使用自己的HTML模板文件** 🎨 |

### 邮件模板变量

邮件主题中可以使用以下变量：

| 变量 | 说明 | 示例 |
|------|------|------|
| `{code}` | 验证码 | `123456` |
| `{groupId}` | QQ群号 | `123456789` |
| `{groupName}` | 群名称 | （待扩展） |
| `{userId}` | 用户QQ号 | `987654321` |
| `{expireTime}` | 验证码有效期（秒） | `300` |

### 自定义邮件模板

如果你想使用自己的HTML邮件模板，可以按照以下步骤操作：

#### 步骤1：创建自定义模板文件

1. 复制参考文件 `config/email-template-example.html`
2. 重命名为你的文件名（如：`my-template.html`）
3. 编辑HTML内容，在需要的地方使用变量（如：`{code}`, `{groupId}` 等）
4. 将文件保存在 `config` 目录下

#### 步骤2：配置使用自定义模板

**方法一：锅巴配置（推荐）**
1. 进入锅巴配置界面
2. 找到“群管配置” → “邮箱验证码配置”
3. 在“邮件模板风格”中选择 **“自定义模板”**
4. 在“自定义模板文件名”中输入你的文件名（如：`my-template.html`）
5. 保存配置

**方法二：编辑配置文件**
编辑 `config/default_config/groupAdmin.yaml`：
```yaml
groupVerify:
  emailVerify:
    emailTemplateStyle: "custom"
    customTemplateFile: "my-template.html"
```

#### 自定义模板示例

参考文件：`config/email-template-example.html`

支持的变量：
- `{code}` - 验证码
- `{groupId}` - QQ群号
- `{userId}` - 用户QQ号
- `{expireTime}` - 有效期（秒）

示例代码片段：
```html
<div>
  <p>群号：{groupId}</p>
  <p>验证码：<strong>{code}</strong></p>
  <p>有效期：{expireTime}秒</p>
</div>
```

#### 注意事项

⚠️ **重要提示**：
1. 自定义模板文件必须放在 `config` 目录下
2. 确保HTML格式正确，建议使用完整的HTML文档结构
3. 所有CSS样式必须使用内联方式（style属性）
4. 如果文件不存在或加载失败，系统会自动使用默认模板
5. 修改模板文件后无需重启，下次发送邮件时会自动重新加载

## 注意事项

1. **安全性**：邮箱授权码是敏感信息，请妥善保管，不要泄露
2. **频率限制**：注意邮箱服务商的发送频率限制，避免被判定为垃圾邮件
3. **QQ邮箱限制**：QQ邮箱每日有发送上限，如果群内加人频繁建议使用企业邮箱
4. **用户隐私**：确保用户同意接收验证邮件
5. **测试发送**：配置完成后建议先测试是否能正常发送邮件

## 常见问题

### Q: 邮件发送失败怎么办？
A: 检查以下几点：
- SMTP配置是否正确
- 授权码是否正确（不是登录密码）
- 网络连接是否正常
- 查看日志中的具体错误信息

### Q: 用户收不到邮件怎么办？
A: 可能原因：
- 邮件被归类到垃圾邮件，让用户检查垃圾箱
- QQ邮箱未开通该功能
- 邮箱服务商延迟，稍等片刻

### Q: 可以自定义验证码长度吗？
A: 可以，在配置中修改 `emailVerify.codeLength` 的值

### Q: 如何切换回数学计算验证？
A: 再次使用 `#切换验证类型` 命令即可

## 命令列表

- `#切换验证类型` - 在数学计算和邮箱验证之间切换（仅主人可用）
- `#开启验证` / `#关闭验证` - 开启或关闭当前群的验证
- `#重新验证 @用户` - 重新验证指定用户
- `#绕过验证 @用户` - 让指定用户通过验证
